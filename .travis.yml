language: python
sudo: false
cache: pip

python:
  - 2.7
  - 3.4
  - 3.5
  - 3.5-dev
  - 3.6
  - 3.6-dev
  - pypy3.3-5.2-alpha1
  - pypy3.5

env:
  - PYTHONHASHSEED=random

install:
  - travis_retry pip install -U pip setuptools patiencediff

script:
  - python -m unittest merge3.test_merge3
